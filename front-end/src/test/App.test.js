import { render } from '@testing-library/react';
import App from '../App.jsx';

describe('App Tests', () => {
  test('should probably test something', () => {
    render(<App />);
  })

  test("However I'm not really sure what or how", () => {
    expect(true).toBeTruthy;
  })
});